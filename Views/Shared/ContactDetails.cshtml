@using AddressBook.Models;
@model Contact

<div class="contact-details">
  <h2>Contact Info</h2>
  <h3>Name</h3>
  <p>@Model.GetName().GetFull()</p>
  <h3>Address</h3>
  <p>@Model.GetAddress().GetStreetAddress()</p>
  <p>@Model.GetAddress().GetFullCityAddress()</p>
</div>
